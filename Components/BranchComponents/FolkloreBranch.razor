@using IncrementalSheep.Components.Buttons

@foreach (var idea in Ideas.Where(i => !i.IsLocked))
{
    <IdeaButton Idea=@idea />
}

@code {
    [Parameter] public required List<Idea> Ideas { get; set; }
}
