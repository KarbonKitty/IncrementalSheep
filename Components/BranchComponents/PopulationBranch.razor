<RecruitButton />

@foreach (var sheep in Sheep)
{
    <SheepButton Sheep=@sheep />
}

@code
{
    [Inject] public required GameEngine Engine { get; set; }
    [Parameter] public required List<Sheep> Sheep { get; set; }
}
